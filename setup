#!/bin/bash

# Create a symbolic link
ln -sf $HOME/dark-paktool/pak $PREFIX/bin/paktool

# Grant execution permissions
chmod +x $HOME/dark-paktool/DARKSIDE
chmod +x $PREFIX/bin/paktool

# Confirm installation
echo "Installation complete! Type 'paktool' to execute the tool."
